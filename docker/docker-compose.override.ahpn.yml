services:
  atom:
    volumes:
      - /Users/orestes/devel/shell-utils/atom-scripts:/mnt/data:rw
    extra_hosts:
      host.docker.internal: host-gateway
    environment:
      - PHP_IDE_CONFIG=serverName=docker
